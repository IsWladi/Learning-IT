[project]
name = "fastapi_with_docker_uv_dev"
version = "0.1.0"
description = "A simple FastAPI application with uv for development running in Docker"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.116.0,<0.117.0"
]
